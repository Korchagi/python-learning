#Задача 10: вложенные циклы (категория → товары)
# входные данные
catalog = {
    "Электроника": ["Ноутбук", "Смартфон", "Наушники"],
    "Бытовая техника": ["Пылесос", "Чайник"],
    "Аксессуары": ["Чехол", "Зарядка", "Кабель"]
}

#расчеты
total_all = 0 #общий счетчик для всех товаров
for category, items in catalog.items(): #внешний цикл по категории
    print(category) #выводим название категории
    for item in items: #запускаем внутренний цикл
        print(f" - {item}") #выводим товар внутри внутреннего цикла
    print(f"Итого:{len(items)} товаров") #строка с итогом
    total_all += len(items) #добавляем к общему счетчику
print(f"Всего товаров в каталоге: {total_all}")
